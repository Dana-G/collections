%h1 Listing Users

%table
  %thead
    %tr
      %th Email

  %tbody
    - @users.each do |user|
      %tr
        %td= user.email
        %td= link_to 'Show', user
%br

= link_to 'New User', new_user_path
